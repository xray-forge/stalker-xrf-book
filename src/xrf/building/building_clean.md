# 🥑 Build clean up

`Clean` is build parameter to purge target `gamedata` directory before starting assets building.
Remove artifacts from the target directory that XRF tools has generated in the past.
