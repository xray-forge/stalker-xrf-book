# Spawn

Provides ALife spawn file utilities.

## Unpack

```powershell
npm run cli -- spawn unpack
```

Options:

- `-p, --path <path>`: source spawn file path.
- `-d, --dest <dest>`: output directory.
- `-v, --verbose`: print verbose logs.
- `-f, --force`: remove an existing unpacked destination first.
