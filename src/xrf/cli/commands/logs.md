# Logs

Prints the last lines from the linked engine log file.

```powershell
npm run cli -- logs
npm run cli -- logs 100
```

The optional argument is the number of lines to print. It defaults to `15`.
