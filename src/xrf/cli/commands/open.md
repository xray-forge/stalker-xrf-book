# Open

Opens configured folders in the system file explorer.

```powershell
npm run cli -- open_game_folder
npm run cli -- open_project_folder
```

`open_game_folder` uses the configured or detected game path. `open_project_folder` opens the engine repository.
